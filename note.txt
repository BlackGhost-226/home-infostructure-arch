neofetch
waybar
hyprland
wlogout
mako

btop
neofetch

added neofetch to ~/.bashrc

--| lazyvim |--
yay -S --noconfirm neovim gcc curl fzf ripgrep fd
git clone https://github.com/LazyVim/starter ~/.config/nvim
rm -rf ~/.config/nvim/.git

--| lazygit |--
yay -S --noconfirm lazygit

raed "your github email: " $EMAIL
ssh-keygen -t ed25519 -b 4096 -C $EMAIL
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
ssh -T git@github.com
